apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# A imagem "sistema-principal-veiculos:local" e construida localmente
# (docker build) e carregada no Minikube com "minikube image load" —
# nunca vem de um registry remoto nesta etapa.
images:
  - name: sistema-principal-veiculos
    newTag: local

patches:
  - target:
      kind: Deployment
      name: sistema-principal-veiculos
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: Never
