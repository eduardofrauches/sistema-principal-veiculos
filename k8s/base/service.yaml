apiVersion: v1
kind: Service
metadata:
  name: sistema-principal-veiculos
  labels:
    app: sistema-principal-veiculos
spec:
  selector:
    app: sistema-principal-veiculos
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  type: ClusterIP
